KB_HOST=0.0.0.0
KB_PORT=8000
KB_DATA_DIR=./data
KB_LOG_LEVEL=INFO
KB_TIMEZONE=Europe/Berlin

# Pfad zum Fernet-Master-Key (32-Byte urlsafe base64).
# Bei leer werden lokal keine Credentials verschlüsselt persistiert.
KB_SECRET_FILE=
